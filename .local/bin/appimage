#!/bin/sh

choose_appimage() {
	ls -d ${HOME}/Desktop/AppImage/*.AppImage \
		| bemenu \
		-i -l18 -p 'AppImage' -P '-->' \
		--tf '#26A69A' --tb '#212121' \
		--hf '#eeeeee' --hb '#005577'
}

APPIMAGE=$(choose_appimage)

if [ ! -z "$APPIMAGE" ]; then
	exec "$APPIMAGE"
fi
